let Point A  = { x : A, y : A };
let IntPoint = Point Int;
let Color    = (Int, Int, Int);
let ColorIntPoint = Point & { c : Color };
let diff (p : IntPoint) (q : IntPoint) = (q.x - p.x, q.y - q.y);
let red = (255,0,0) and blue = (0,0,255);
let origin = { x = 0, y = 0 } and vertex = { x = 6, y = 4 };
diff (origin ,, { c = red }) (vertex ,, { c = blue })